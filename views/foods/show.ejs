<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= pantry.name %></title>
</head>
<body>
  <%- include('../partials/_navbar.ejs') %>
  <h1>Item: <%= pantry.name %></h1>

  <% if (pantry.ingredients) { %>
    <p>Ingredients: <%= pantry.ingredients %></p>
  <% } %>
  <a href="/users/<%= user._id %>/foods/<%= pantry._id %>/edit" class="edit-link">
    Modify this item
  </a>
  <form action="/users/<%=user._id%>/foods/<%=pantry._id%>?_method=DELETE" method="POST">
    <button type="submit">Delete </button>
</body>
</html>